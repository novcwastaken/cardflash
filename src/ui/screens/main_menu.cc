#include "imgui.h"

namespace CardflashUI {
    /// The main menu (wow)
    void MainMenu() {
        ImGui::Begin("Main Menu");
        ImGui::Text("Welcome!");
        ImGui::Text("balls.");
        ImGui::End();
    }
}